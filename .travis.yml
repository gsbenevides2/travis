language: node_js
node_js:
 - node
 - lts/*
jobs:
 include:
  - stage: deploy
    script:
     - yarn global add expo-cli
     - expo login -u $EXPO_USERNAME -p $EXPO_PASSWORD
     - expo publish --non-interactive
